#[cfg(test)]
mod blockchain_validation {
    use gold_2::*;

    #[test]
    fn validate_block() {
        let account_set = create_dummy_account_set();
        let name_set = create_dummy_name_set();
    }
}
